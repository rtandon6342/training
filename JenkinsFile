pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git init 
                git clone https://github.com/rtandon6342/training.git
            }
        }
    }
}
