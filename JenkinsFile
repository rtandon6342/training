pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git clone https://github.com/rtandon6342/training.git
            }
        }

        stage('Hello_Again_test') {
            steps {
                sudo cp index.html /var/www/html/index.html
            }
        }
    }
}
